#include<stdio.h>
#include<stdlib.h>
int main()
{
	int i, a,b,count=0,total_numbers;
	printf("Enter the value of and b\n");
	scanf("%d%d",&a,&b);  
	 
		{
		printf(	"total even numbers=%d",count);
      exit(0);
  }
	printf("numbers\t count");
    for (i>a; i< b; i++)
	{ 
	if( i%2==0)
	{
	count++;
	{
	if(count<=1)
	 printf("%d \t %d\n",i ,count);
}

}

	return 0;
}
